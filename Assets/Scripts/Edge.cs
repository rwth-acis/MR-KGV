// Script is attached to each edge element of the displayed graph.
// It is used to manage the properties of the edge, such as
// label, weight, object, subject, ...

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Edge : MonoBehaviour {
    // Start is called before the first frame update
    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }
}
