// Script is attached to each node element of the displayed graph.
// It is used to manage the properties of the node, such as
// label, annotation, representation type, ...

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Node : MonoBehaviour {

    string label;
    string annotation;
    int representationType;

    // Start is called before the first frame update
    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }

}
